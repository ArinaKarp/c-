{
    "configurations": [
        {
            "name": "Launch Server",
            "type": "cppdbg",
            "cwd": ".",
            "request": "launch",
            "program": "${workspaceRoot}/build/websocket-server-sync",
            "args": ["127.0.0.1", "8080"],
        },
    ]
}